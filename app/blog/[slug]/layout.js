export const metadata = {
  title: "Carnival Trips - Blog Details",
  description: "Find the latest blog posts and updates from Carnival Trips.",
};

export default function BlogDetailsLayout({ children }) {
  return <div>{children}</div>;
}
